{% extends 'base.html.twig' %}

{% block title %}Lista de Livros{% endblock %}

{% block body %}
    <h1>Lista de Livros</h1>
    <table class="table">
        <thead>
        <tr>
            <th>ID</th>
            <th>Título</th>
            <th>Autor</th>
        </tr>
        </thead>
        <tbody>
        {% for livro in livros %}
            <tr>
                <td>{{ livro.id }}</td>
                <td>{{ livro.titulo }}</td>
                <td>{{ livro.autor }}</td>
                <td>
                    <a href="{{ path('livro_adicionar') }}" class="btn btn-primary">Adicionar livro</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Nenhum livro encontrado.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}